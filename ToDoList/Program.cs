using System;
using System.Linq;
using MySql.Data.MySqlClient;

namespace ToDoList
{
    class Program
    {



        static void Main(string[] args)
        {

            //DBConnection myConnection = DBConnection.GetSingletonInstance();

            //String[] sqlCommands = {
            //    "INSERT INTO Item (itemId, itemName, done) VALUES ('9', 'Candy', '0')",
            //};

            //myConnection.OutputDB();
            //myConnection.RunSqlTransaction(sqlCommands);
            //myConnection.OutputDB();
            //myConnection.Close();



            //itemOperations.GetAllItems().ForEach(item => Console.WriteLine(item.ToString()));

            //itemOperations.DeleteItem(new Item(9, "Fanta", false));

            //itemOperations.GetAllItems().ForEach(item => Console.WriteLine(item.ToString()));
        }


    }
}
